import GeneratePassword from "./components/GeneratePassword";

function App() {
  return (
    <div
      style={{
        height: "100vh",
        background: "#00203F",
      }}
    >
      <GeneratePassword />
    </div>
  );
}

export default App;
